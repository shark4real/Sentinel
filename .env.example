# Tambo API key (get one at https://tambo.co/dashboard)
# When set, enables Tambo cloud AI for component selection.
# When absent, Sentinel runs in local analysis mode (fully functional demo).
VITE_TAMBO_API_KEY=
